create table employee{
	id int primary key,
	name varchar(50),
	email varchar(100);
	number double;
	date_of_joining date;
	gender enum("Female","Male")
	author varchar(255) not null,
	primary key (id)
}